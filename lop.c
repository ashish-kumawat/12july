#include<stdio.h>
int main ()
{
	int num;
	printf("enter the number");
	scanf("%d",&num);
	int rem,sum=0;
	while(num> n0)
	{
		rem=num%10;
		sum=sum*10+rem;
		num=num/10;
		}
		
		printf("the reverse number is :%d",sum);
			
	return 0;
}
